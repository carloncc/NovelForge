# NovelForge 变更日志

所有值得注意的项目更改都将记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，
并且本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [Unreleased]

### 新增
- 每部作品主题（主题色随画风）：组装时从风格锚点/首张背景/CG 取色，生成封面配色与每局 `--nf-accent` 变量；按界面语言切换字体栈（en/ja/繁中不再强行思源宋体 CN）；字体/可访问性焦点环/窄屏快捷栏图标化在组装时注入 `userStyleSheet.css`（幂等追加，重跑不叠加）
- 导出页新增「标题画面」卡片：封面与 Logo 支持 自动生成 / 不使用 / 自定义图片 三种模式、标题曲可选可关、继续/流程图/鉴赏室开关、主题取色开关，保存后自动重新组装（纯本地、免费）
- 导出游戏内容补齐：按实际章节自动生成流程图（替换模板 demo 节点）、输出 `build.json` 构建信息与 `game/HOW_TO_PLAY.txt` 上手指南、PWA 名称与图标替换为作品封面
- 导出游戏演出增强：场景级电影黑边与影调滤镜（紧张/战斗→`filmMode`+`godrayFilm`，回忆/梦境→`oldFilm`，按值变化输出不刷屏）
- 鉴赏室扩展：CG 按章节筛选、IntersectionObserver 懒加载、灯箱左右切换 + 键盘 ←/→/Esc/± + 滚轮/双击缩放、角色图鉴点击跳转立绘鉴赏、顶部收集度统计（角色/CG/音乐）
- 生成页新增失败任务横幅：实时显示失败数量，可就地查看失败项或一键按失败原因重试；预览页支持 16:9 舞台、50%–150% 缩放与全屏试玩
- 单阶段重跑后自动补齐下游：剧本/图像/配音任一阶段重跑成功，自动盘点缺失的图像、配音与组装，确认一次后连续补齐（复用缓存只补缺失，图像/配音计费，组装免费），避免一个个手动再点；分章/翻译/提取等文本上游不触发
- 生成页新增「跑完自动补齐下游」总开关（旧项目缺该字段按开启处理）；关闭后恢复旧的严格单阶段语义
- 导出游戏标题视觉自动生成：组装时用 Canvas 绘制封面（1920×1080 渐变 + 标题排版）与文字 Logo，写入 `game/background/` 并挂到 `Title_img` / `Game_Logo`（导出页「应用设置」会保留 Logo）；没有画像的示例项目不再是纯色空标题页（无 DOM 环境自动跳过并回退「第一章 CG / 首张背景」）

### 修改
- BGM 匹配规则改为中英双语关键词（battle/ambient/sad/happy/mystery/dawn…）并按「同氛围稳定轮换」选曲（同一场景每次组装结果一致，不再所有场景永远用第一首）；鉴赏室 BGM 显示可读氛围标签（如「安静 · calm_piano_02」）
- 长台词配音按句拆分多次合成（与剧本显示全文一致，不再硬截断前 500 字）；多段文件以 `_pN` 后缀区分，超长阈值与校对提示统一到同一常量
- 生成页重构为「四段结构」（项目栏 → 生成内容 → 生成 → 结果）：范围切换只替换第三段，不再让整页随模式换掉；顶部不再有文案/语义随模式漂移的全局主按钮
- 「逐章生成」成为默认范围：单章 / 多选「生成选中(N)」/ 顺序补全未完成 / 追加新章 / 逐章意见与全量 / 可选含配音；去掉「确认分章」硬门槛（分章状态降级为徽章与可核对信息），缺角色卡时就地补提取
- 同一语义选项只出现一次（`useImage` / `useTts` 在级联面板改为只读状态）；结果区 8 个 tab 按「运行 / 产物」分组，日志 tab 不再靠 `v-else` 兜底；空态改为单一提示 + 就地动作
- 代码结构：`GeneratePage.vue` 3722 → 369 行；新增 `src/components/generate/` 七个区块组件、`src/stores/generate.ts`（运行态单一来源，范围/结果 tab 选择可跨页面保留）与 `src/stores/nav.ts`
- 单章重跑不再静默跳过图片：视觉守门待确认时，跑完会明确提示「该章图片未生成」并记好待补阶段（去视觉守门批准后自动补图＋重新组装，不抢当前页面）；「顺序生成未完成」队列照常逐章出剧本，但收尾不再谎报「全部生成完毕」
- 视觉守门面板排版与样式重做：三视图 hero 预览固定 240px（中屏 200px）不再被压扁、按钮不再竖排折行、缺失缩略图虚线占位、成本/服装行两端对齐、批准栏吸底并补齐 900px 中屏断点
- 生成游戏的入口/标题界面精修：入口英文提示改为「点击任意处开始」并加呼吸动效；标题页 Logo 容器由引擎的全屏拉伸改为居中固定尺寸（同时解除它的全屏点击遮挡——此前会挡住「开始游戏」，点不进游戏）；封面标题排版移到右侧，避开左侧菜单按钮栏
- 对白文本框高度按内容收紧（430 → 320 起）；底部快捷操作栏紧凑化并支持窄屏折行（≤1200px 换行、≤780px 进一步缩小），避免手机导出时一组图标横向溢出
- 鉴赏室（appreciation.html）主题化：启用暗色 `color-scheme`，自定义下拉/滑杆/复选框/按钮/音频播放器样式，补充空态引导文案，表情与服装显示中文标签（normal → 默认 等）

### 修复
- 导出游戏入场首屏/字体/流程图核对（ReqFlow 清单第一批实查）：`Title_img`/`Game_Logo` 仅在有文件时才写入（缺失会让入口卡白屏）；流程图/字体栈/入口标题/焦点环按作品生成；复核确认清单中「Enable_Save/Load/QuickSave/Options 等开关」在引擎 4.6.3 不存在（全包 0 引用），该项不适用，「继续/流程图/鉴赏室」三项开关已可配置；右键菜单/backlog 美化、Live2D、字体子集化、md/docx/epub 导入、原子写等大特性转入后续排期
- 个别中转站（日志里的 `gpt-image-2.5` 线路）只开放纯文本 `/v1/images/generations`：请求体一带参考图就撞上服务端 256 KiB 上限，回执「which this route does not support / No image inputs, edits」，于是该角色剩下的立绘/表情任务**每次运行都固定失败**（日志中稳定失败的 8 个 413 任务）。现把这种回执归一成类型化 `REFERENCE_UNSUPPORTED`：不再重试、不再改写提示词，也绝不静默丢掉参考图（丢图会让角色形象不一致），并在任务日志里直接给出可执行的修复提示（在「API 配置 > 图片生成」对该线路重点一次「测试连接」让程序自动修正能力，或改选支持图生图的线路）
- 点「批准」后全书图片被清空、1 章任务却跑满全书（本次线上事故根因，日志证据 `assets.backup-2026-09-13T11-21-52.json` 图片全为 0）：视觉指纹把小说正文/分章也算进去，只改正文也会被判成「视觉输入全变」；`approveVisualBible` 在没有显式作废范围时又回退成 `global`，把 `cacheBinding.globalFingerprint` 推成新指纹、与磁盘缓存标记对不上 → 图像阶段判「整书缓存过期」→ 单章模式也全书重画（9 个角色三视图被逐个重画，终止只能停在任务边界）。现改为：批准只在有显式/推断范围时作废，且**保留旧的 `globalFingerprint`**（现存图片本来就是按旧指纹画的）；新增「视觉输入签名」（画风 ＋ 逐角色哈希，不含正文/分章）作为批准基线，指纹变化时用它判断是真有画图输入变了，还是只是小说正文变了——后者**一张图都不删**
- OpenAI 兼容通道访问 opencode.ai「OpenCode Go」时自动携带稳定 `x-opencode-session` 与自定义 User-Agent，修复该网关返回 400 MissingSessionID 无法调用的问题
- 支持在通道配置 `extra.headers` 里自定义请求头（可覆盖默认 Authorization / User-Agent 等）
- 重启后「单章节生成 / 本次重跑」整块消失、只剩一句「还没有小说」：`project_state.json` 只存章节标题不存正文，源小说路径失效后恢复时 `novel` 直接变 null；现源文件不可读时用 `.novel2vn/split.json` 分章缓存重建小说（重建会写日志＋页面提示），逐章生成入口不再丢
- 重启后「单阶段重跑」显示「还没有生成结果」、素材页空白、视觉守门按钮拿不到卡片：`lastResult` 只认 `project_state.json` 快照，而快照是上一次保存时的旧值，生成没跑完就退出等于没有；现以磁盘工作副本为准，快照缺失时用 `.novel2vn/cards.json` ＋ `meta.json` 重建
- 视觉守门「刚批准就失效 → 图片一张也生不出来」：批准时按快照卡片算指纹、图像阶段按磁盘卡片复算，两次输入对不上就直接判「输入已变化」；现两处统一以 `cards.json` 为准，打开视觉守门面板也会先对齐一次指纹
- 换过小说 / 重新提取后，视觉守门清单里残留的旧角色（已不在卡片里）永远拿不到 approved 标记，每次读清单都把已批准的守门打回 stale：现只校验当前卡片里的角色（`loadVisualBible` 新增可选角色范围参数，省略即保持旧行为）
- App 启动恢复项目状态失败不再静默：会记一条错误日志并在页面留下警告，不再只留一个空白的「还没有小说」而没有任何线索
- 单章模式下「终止」不生效：终止只拦「还没派发的任务」，已在跑的任务（视觉描述 / 生成 / 重试 / 60 秒退避）会继续烧钱跑完；现图像任务全程响应终止（描述前、付费生成前、重试前、退避等待各检查一次，退避改为可中断等待），终止后不再派发新任务、不再刷新进度，界面显示「正在停止…」
- 章节行的「图像 x/y」数字不对（用户报告的 76/91）：旧口径把**全书**所有角色的三视图/立绘/表情/动作都算进每一章（于是每章都是同一个大数字），又因为给任务构建器传了 `items: []` 而永远漏掉物品图；现按**本章**收窄为「本章背景/CG ＋ 本章出场角色的人物图 ＋ 本章引用的物品图」，各章数字随出场范围变化，「全选未完成」与队列也只补真正缺的章
- 章节行误报「剧本·旧缓存」，并用过期剧本算出整章假缺失：挑缓存用 `localeCompare` 排序时 `script_chN_xxx_t<指纹>.json` 排在 `script_chN_xxx.json` 之前，旧版无指纹缓存会在循环后面**覆盖**掉刚匹配上的新剧本；现加守卫，只有该章没有任何指纹匹配结果时才退回旧缓存（现网 22 章全部命中指纹缓存，不再有假「旧缓存」）
- 逐章工作台新增旧缓存提示：仍有章节只能退回旧版无指纹剧本时，明确写出「共 N 章」并说明图像数是按旧剧本估算、建议先重跑这些章的「剧本」

### 修复（立绘改为全身构图 2026-09-18）
- 立绘/表情/服装/动作从「半身（thighs-up）」改回「全身 head to feet」：此前代码为追求舞台人物更大而强制半身，导致角色卡生成半身照片；现提示词明确要求全身、含鞋子、不裁切
- 人物类任务（立绘/表情/服装/动作/三视图）追加构图负面词：`half body, bust shot, waist-up, close-up, cropped head/legs/feet, out of frame`，防止模型自行裁成半身
- 提取提示词同步加固：imagePrompt 必须写 `full body, head to toe, entire figure in frame, shoes visible`，禁止 `half body / portrait / close-up / waist-up` 构图词
- 缓存失效：旧图元数据没有负面词记录，现在「期望有负面词时必须一致」→ 旧的半身立绘会在下次图像阶段自动判为未命中并重画（一次性的立绘重生成成本）
- 新增回归测试 `tests/unit-figure-fullbody.ts`（不得出现 thighs-up、必须 head to feet 与排除半身措辞）

### 新增（AI 选音色 2026-09-18）
- 角色卡编辑新增「AI 选音色」：一键让文本 LLM 按 性别 / 性格 / 音色描述 从 TTS 音色库重新分配音色；已绑定克隆音色的角色自动跳过
- 分配结果强校验（纯函数 + 单测）：音色必须在库内、性别必须匹配（男声不会分给女角色）、主要角色尽量一人一色（撞色自动改挑未占用的同性别音色）、未知角色 id 丢弃
- 说明：提取角色卡时管线本来就会让 AI 选一次音色（含性别约束与兜底），这个按钮用于不满意时随时重选；改完记得点「保存卡片」

### 修复（生成页日志与进度可见性 2026-09-18）
- 「设置」抽屉与其中的「生成内容」改为默认展开（此前默认收起，配置项一进来反而看不到）；「生成结果与产物」保持默认收起
- 运行日志恢复为主页面常驻卡片（此前被收进产物抽屉，看起来像被删了）：生成时实时滚动、最多 260px、带保存/复制
- 进度醒目化：运行时显示吸顶进度卡——当前阶段名 + 百分比（有细粒度进度时）或「阶段 x/7」+ 全宽进度条 + 步骤条；无细粒度进度的阶段（分章/提取/组装）进度条为呼吸态，不再显示成卡在 0%
- 运行完成后不再自动切到「卡片编辑」并弹开产物抽屉（进度与日志已在主页面，产物抽屉保持收起）

### 修改（生成页大改版：主页面只剩「章节列表 + 一个按钮」2026-09-18）
- 设置抽屉：「项目目录 / 生成方式（逐章补全·整书生成·单阶段重跑）/ 生成内容与高级」全部收进一行「设置」折叠，摘要直接显示 小说名 · 方式 · 图像✓ 配音×；子面板去卡片外壳，不再卡片套卡片
- 产物抽屉：整个结果区（运行/剧本/素材与视频/卡片/视觉守门）从页面下半部收进一行「生成结果与产物」，摘要显示 `剧本 0/3 · 无失败 · 尚未组装`；程序性跳转（视觉守门待确认/失败处理/卡片编辑）会自动展开；运行中把实时进度行移到主页面步骤条下方
- 主页面现在只有：状态横幅（条件出现）→ 步骤进度（运行时）→ 设置折叠行 → 逐章列表（勾选+标题+中文状态词+字数+重跑）→ 唯一主按钮 → 产物折叠行；可见操作从 20+ 降到个位数
- 这样生成页的默认视野是一个「清单 + 下一步」，符合 Hick 定律与渐进式披露（一级完成主任务，二级才是设置与产物）

### 修改（按调研结论继续精简 2026-09-18）
- 结果区标签按「分类优先」合并为 5 个：运行（状态与费用 + 失败项 + 日志）、产物（剧本、素材与视频）、设定（卡片编辑、视觉守门）；日志与失败项不再各占一个标签（原 8 个平铺标签，Hick 定律：分组降低一级决策量）
- 阶段重跑页标题从一整句长说明改为「分阶段状态」，重跑口径与计费说明收进「说明：重跑口径与计费」折叠（去重复文案）
- 去掉与面板内容重复的范围提示行；页面副标题改为任务导向（选范围 → 生成 → 试玩 → 导出）
- 依据：Hick 定律 / 渐进式披露（NN/g：一级完成 80% 任务、最多两级、门牌要写清内容）/ Kibana 页头治理（每页 1 个主操作 + 有限次操作，其余收进菜单）/ anti-ui-slop 蒸馏法（去重复文案、去重复操作、去无分组价值的容器、一个主操作）

### 修改（生成页状态可读性重写 2026-09-18）
- 逐章列表重写：每章只显示 勾选 + 标题 + 一个中文状态词 + 字数 + 「重跑」链接。状态词统一为：**未生成 / 旧缓存 / 缺图 x/y / 缺配音 x/y / 已完成**（此前「剧本× 图像— 配音—」符号堆叠，且「配置关闭不适用」与「未生成」无法区分，完全看不出哪些做完了）
- 修复章节标题重复前缀：标题已含「第X章」时不再拼成「第1章 第一章 黄昏的城门」（逐章列表与保真核对两处）
- 项目栏收成一行折叠摘要（默认收起）：状态徽章与输出目录显示在摘要行，输入框/浏览/加载仅在展开后出现，且按钮降级为文本链接（此前「浏览…」被渲染成一条超宽假输入框）
- 「追加新章节」移入「分章设置与意见」；失败项标签页合并进「运行」（结果区标签 8 → 7）
- 上一批已落地：生成页唯一主按钮（补全未完成/生成选中/开始生成）、逐章意见/全量/分项重跑收进「重跑」折叠区、分章操作收进折叠区、整书高级项收进「高级」、小按钮全站降级为软底次级样式

### 修改（生成页单主按钮改版 2026-09-18）
- 全局视觉分级：`.btn.small` 统一降级为软底次级样式（此前小按钮也是渐变主按钮色，一屏十几个「主按钮」，页面像操作台）；全站只有全尺寸 `.btn` 是主按钮，危险操作保留红色软底；新增 `.link-btn` 文本链接样式承载降级入口
- 生成页收敛为单一主按钮（随范围变化）：「补全未完成（N）」/「生成选中（N）」/「开始生成」；逐章行默认只显示 勾选/章节/状态/字数，逐章意见、全量、分项重跑（剧本/图像/配音）、停用收进该行的「重跑」折叠区（能力保留、视觉降噪）；分章操作（AI 分章/意见/碎章阈值/特殊章节/标记已核对）收进「分章设置与意见」；整书面板的 Agent 模式/提取分段/章节重跑收进「高级」折叠区；「生成内容」折叠摘要显示关键开关（图像/配音/视频位/BGM/SE）
- 导出页保持原有布局（打开文件夹 / 打包 / 复制路径 / 保存并重新组装 / 运行检查 / 分发卡片），本次仅修内部问题：导出检查可等待（见下）、界面语言初始化（见下）、网页打包改走 Worker 直下载
- 修复：导出「界面语言」此前未从项目选项初始化，且保存时会无条件写回 `options.language`（会把翻译目标静默重置为 zh_CN）；现初始化自选项、并在导出时才写回
- 修复：导出检查存在竞态（自动检查在途时再次触发会立即返回且报告为 null，打包可能在没有完整检查的情况下继续）；现检查 Promise 可等待，打包流程 await 同一实例，检查失败（异常）与检查不通过都会中止打包
- 修复：网页版模板解析会为不存在的候选路径探测远端，控制台刷 8 条 404；现只对模板根的直接子项做远端探测
- 修复：`safeCallResult` 对二进制返回值（ArrayBuffer/TypedArray）改为只记录字节数，防止大对象被日志序列化拖死（导出 zip 47MB 曾因此耗时约 90 秒）

### 修复（导出卡死 / 配音性别 / 按钮精简 2026-09-18）
- 修复：网页版「打包 zip」卡死页面——原来在主线程 `zipSync` 压缩整个项目（含引擎与字体），且 zip 字节经 tauri 日志包装层被 `truncate` 序列化（47MB 实测拖到 90 秒）。现改为专用 Web Worker 压缩、媒体/字体文件按 store 模式跳过二次压缩、压缩完直接触发浏览器下载（不经日志层）；实测 47MB / 125 文件 212ms 完成并正常下载
- 修复：女角色配音拿到男声——音色兜底一律取列表第一个（默认列表首个是男声），性别纠错又靠 ID 前缀正则（认不出 `Chinese (Mandarin)_*`、`harry`/`anna` 等）。现音色性别统一由 MiniMax 官方表 + 常见音色名判定，AI 提取与配音任务两处都按角色性别从音色库稳定挑选（同角色结果可复现），老项目重跑配音会自动纠正
- 优化：导出页按钮精简（高内聚）——「应用设置」与「保存并重新组装」合并为一个保存入口（标题/GameKey/语言/封面/Logo/标题曲/菜单开关一次写入并本地重跑组装）、去掉重复的打包按钮（只保留分发卡片里一个）、打开页面与打包前自动做导出检查（保留手动「运行检查」）
- 优化：生成页按钮精简——失败横幅合并为「处理失败项」一个入口（重试在失败项面板内）、逐章行 5 个按钮收成「生成本章 + 分项（展开后含剧本/图像/配音/全量/停用）」、章节与阶段全选改为单按钮开关
- 新增单测：`tests/unit-voice-gender.ts`（官方表/常见音色名判定、提取归一化纠错、配音任务最终防线）、`tests/unit-web-zip.ts`（网页打包排除缓存、媒体低压缩、zip 内容可解）

### 清理与修复（全链路审计 2026-09-18）
- 修复：内置音效（SE）发布包缺文件——`prepare:template` 会裁掉模板 `game/vocal` 且不回填，现从 `src/gameExtra/se` 幂等同步 8 个 `se_*.wav`，全新检出 / CI 构建的包开启 SE 不再引用不存在的音频
- 修复：导出检查（lint）白名单补齐 `filmMode` / `setFilter`（此前被当作台词解析、跳过素材校验并虚增台词数），并按 render 实际输出收紧白名单（移除无人产生的指令），补单测
- 修复：导出游戏 `index.html` 页面标题改为作品名（此前 PWA 名称已替换，但浏览器标签/收藏仍是 WebGAL）
- 修复：补全未定义的样式变量（`--accent-2` → `--accent`、`--success` → `--ok`），素材行、阶段状态点、抠图状态的文字/圆点颜色不再失效
- 清理：删除死导出与死参数（`jsonKey`/`slugify`/`isReferenceDescriptionDisabled`/`splitChaptersForRestore`/`isWeb`/`DEFAULT_PRICES`/`imageSeedFor`/`figurePerCharacter`/`retryDelayMs`/`RenderOptions.title·gameKey`/`PipelineEvent.costDelta` 等）、无用 import 与 legacy re-export
- 清理：合并重复实现——`fileToBase64`（4 处）、`b64encode`（2 处）、`imageMimeForPath`（2 处）收敛到 `utils/file.ts` / `utils/base64.ts` / `utils/mime.ts`；同一文件重名的 `visualBibleArtifactPath`/`stableHash`/`authHeaders`/`normalizePath` 改名区分；修掉 render 内的变量遮蔽与 generate 中 8 处重复嵌套判断
- 清理：删除 150 个未使用的解构绑定（生成页重构遗留）及未使用 import（导出页/视觉守门/卡片编辑等）
- 清理：删除遗留文件与脚本（`stable-style/`、`OPTIMIZATION_*.md`、`QUICK_FIX.md`、`verify-optimization.ps1`、`scripts/serve-game-tmp.mjs`、`scripts/run-ts-loader.mjs`、未挂载的 `src-tauri/src/config.rs`、无引用的示例与旧测试脚本）及全部临时草稿

### 修复（生成页第二轮排查）
- 新项目不再出现「假结果」：打开生成页即显示「已有生成结果 / 状态=已完成」、折叠「生成内容」、把空壳 `lastResult` 当成已有项目的问题已修（`ensureLiveResultShape` 只在磁盘上确实有卡片/剧本缓存/组装产物时才补结构，并同时识别 `cards_demo.json`）；视觉守门的「已准备」判断也改为按真实卡片数
- 章节状态灯不再虚报：未配置 TTS 时不再显示「配音 0/N」（`voiceSkipped` 现按 API Key 判定）；未配置图像 API 时图像不参与章节完成判定（否则「顺序补全未完成」永远收敛不了，每点一次都空转），队列收尾也会明说「未配置图像 API，图片未生成」
- 「顺序补全未完成 / 生成选中」修复三处队列缺陷：①在状态刷新 await 期间重复点击会二次进入、两个队列互相把对方停掉（改为任何 await 之前同步占队列，并用令牌保证旧队列的收尾不清掉新队列状态）；②换项目 / 重分章 / 停用章节后残留的章节选择索引会指向别的章（现按当前章节集合剪枝、换项目即清空）；③队列逐章组装在「已有成品但部分章节还没剧本」时被组装保护逻辑拦下、首章一失败整队停死——现改为逐章只跑内容、队列结束统一组装一次（渐进式，中途停止也组装已完成部分），单章「生成本章」遇到该保护时也不再谎报失败
- 视觉守门批准后续跑不再静默丢参数：此前只记住阶段与章节范围，用户点过的「全量」、填写的意见、以及全局章节勾选都会被降级成「只补缺失 / 全书」；现原样保存并在续跑确认框里复述（含全量阶段），批准按钮在生成/队列运行中禁用，避免两条管线同时写同一项目
- 「追加新章节」在视觉守门已批准时不再必然失败：追加会改变小说输入、守门指纹随之失效，旧逻辑在批准后仍先跑图像阶段导致中途抛错、内存小说不更新；现追加一律先文本（分章/提取/剧本），守门创建或重新确认后按新增章范围自动补齐图像
- 单阶段「分章」在没有卡片的项目不再「先干完再报错」：自动附带「提取」兜底；「视频推荐点数上限」填 0 恢复为不限制（此前 0 被当成上限 0，默认设置下所有视频推荐位被删光）
- 素材页重生成修复：抠图完成后重新组装不再被自身占用挡住（此前必静默失败）；「重新生成三视图（联动全部）」现在真的覆盖全部动作（此前只重画前 2 个动作）并遵循核心/完整档表情口径；「补全缺失图片」不再把批准守门后存放在 visual-bible 目录的三视图误判成缺失重画，且扫描期间即上锁防双击重复生成；未配置图像 API 时各图像重生成入口会明确报错（不再显示「已重新生成 0 项」）；批准守门下在素材页重画三视图会提示去守门页同步锚点
- 素材/剧本/配音面板细节：停用章节不再出现在剧本逐章重写列表、队列运行中禁用逐章按钮；核对页「接受建议 / 删除该句」同步内存剧本（配音表与单句重配立即按新说话人/行号生效）；切换结果 tab、筛选或旁白开关时停止试听并复位「⏸ 停止」；配音筛选变化重置分页、换项目重置筛选；失败项列表改用稳定 key，「定位重试」直接切到整书生成并重置旧章节范围；日志列表 key 加序号、底部提示共享一个计时器（不再互相提前清掉）
- 清理死代码/死 UI：单阶段面板里写死勾选且无人读取的「补齐组装（本地，免费）」假开关、`GeneratePage.vue` 中已随重构失效的数十个模块导入、store 中未使用的导入/导出；生成中步骤指示器把本次未跑的阶段显示为「跳过」，不再把「分章/翻译/提取」显示成已完成

### 修复（单章生成专项）
- 单章剧本生成失败不再绕过「组装保护」：生成失败与空结果的章节现在也计入"缺剧本"名单——此前只有"未勾选且无缓存"的章节会被保护看到，于是带意见/全量重写某章失败后，同一次运行的组装会按内存章节重写游戏目录，把这一章的旧场景文件删掉（真实数据损失）
- 单章成功/失败汇报如实：本次运行有失败项时不再记「已重新生成 / 预览已组装」；本章剧本失败会明确报「本章未完成」并让队列停下，而不是把管线"部分失败也算成功"当成成功
- 「只重跑图像 / 配音」在组装被保护逻辑拦下时不再误报失败：与剧本部分同口径容错（该部分已成功，只是为避免游戏缩水跳过组装）
- 意见文本与「全量」勾选只在真的跑成功后消费：失败时保留，用户可直接再点一次重试（此前失败也会被清空）
- 队列收尾更安全、更如实：只有整批章节都成功才统一组装；有失败或中途停止时跳过组装（残缺章节集会覆盖游戏目录、删掉未完成章节的旧场景），并在停止/完成汇报里带上本次失败项统计
- 组装前的缺剧本核对覆盖所有路径：含组装阶段的任何运行（视觉守门批准后的自动续跑、手动点「组装」等）都会先核对「启用章节 vs 本次实际章节」，有缺席时明确警告"这些章节会被移出游戏"；要求强制覆盖的单章链路仍直接拦下（此前 assemble-only 的自动路径完全绕过保护，可能静默缩水）
- 单章图像任务裁剪修复：现在要「三视图 ＋ 全部表情/服装/动作」都有成品才跳过该角色——此前只看三视图，会把缺表情差分、新增服装/动作的角色一起裁掉，章节灯显示差图但单章与队列都永远补不上
- 单章人物/物品图按「本次章节出场范围」收窄：只构建本章真正用到的角色/物品任务（与章节灯的计数口径一致），未出场角色的图在对应章节生成时再补，避免「生成本章」连带生成全书人物图
- 收尾减负：去掉单章/队列运行后的重复章节状态全量刷新；剧本文件列表只在剧本页打开时重读，不再每章收尾都读一遍 `game/scene/*.txt`

### 修复（ReqFlow 批次 2026-09-20）
- #780 视觉自检假阴性：模型通过时也会复述检查项（「符合，人物无畸形」），旧正则「含负面词即不通过」把合格图判失败并触发重生成循环；改为「未被否定的负面词才判失败」（无畸形/没有变形/未见明显变形均放行），抽出纯函数并补 15 条单测
- #787 CI 单测在非中文环境必失败：`unit-web-security` 断言中文错误文案，Runner 上语言解析为 en 时必红、tag 发布会卡在 needs:test；改为断言错误类型与状态码（导出 `ProxyHttpError`，语言无关）
- #788 AI 选音色/提取读错音色库：三处调用点传的是文本 LLM 配置，`voiceLibraryFor(llm)` 永远落到默认 17 音色，自定义音色库（MiniMax 等）时全员回退同一音色；现改为显式接收 TTS 配置的音色库（管线/编辑页传入 `input.tts`/`activeConfig("tts")`）
- #789 跨章演出残留：上一章的 BGM / 电影黑边 / 影调滤镜会带进下一章（引擎 changeScene 不清舞台）；每章开头显式复位（`bgm:none`、`filmMode:none`、两种滤镜归零），补回归测试
- #790 PWA 图标尺寸不符：1920×1080 封面被原样改名当 192/512/maskable/180 图标，浏览器会拒绝安装；改为 Canvas 按声明尺寸重绘（contain + 封面均值底色，maskable 收 80% 安全区），非浏览器环境回退旧拷贝
- #791 Web 代理可打内网：同源脚本被注入时可借代理探测内网与云元数据；目标主机增加私网/链路本地黑名单（169.254/10./172.16-31/192.168/100.64/CGNAT/fe80::/fc00::/IPv4 映射），回环保留（Ollama 等本机模型服务）
- #792 章节灯/阶段灯 demo 与正式卡片不隔离：演示模式可能按正式 `cards.json` 算任务数，出现演示模式下永远收敛不了的缺失；现按当前模式优先取对应文件（章节灯）与只看对应文件（阶段灯）
- #779 模板校验缺项：`check:template` 不校验 SE 与主题回填，模板陈旧时构建静默产出无声效/无主题游戏；现校验 `game/vocal/se_*.wav`、`userStyleSheet.css` 非空与配音中断定制，缺失即 fail 并提示 `pnpm prepare:template`
- #781 翻译不分块且输出预算不扣输入：长章节/小上下文模型必失败或被截断；现按 `inputCharBudgetForText` 分段翻译后拼接，输出上限改为「上下文 − 输入估算 − 余量」（新增共用 `outputTokensForText`）
- #783 崩溃残留不清理：原子写/替换被强退打断会留下 `.tmp` 与 `.replace-backup` 永久占盘；新增 Rust `cleanup_stale_files`（启动时扫描项目目录：删本程序 `.tmp`、恢复中断的 `.replace-backup`、清理已完成替换的备份），含单测
- #785 `withRetry` 不可达的 `throw lastErr` 死代码删除；#786 停止录音后置空 `recorderRef`，避免残留 inactive MediaRecorder 被下一轮复用
- #782 预览服务器连点启动竞态：旧实现先绑定再释放旧实例，第二次点「启动/刷新」会报端口占用；改为先 stop + 释放监听 socket 再绑定
- #536 默认开启 BGM/SE：新项目此前默认静音（标题与全场景无声、鉴赏室音乐页永远为空）；现默认「有文件就播」，可随时关闭，测试同步更新
- #637 提取补全不分片：一次请求补全全部卡，卡多必截断/漏回卡留空壳；现按 JSON 体积装箱分批补全（单卡超预算独占一批），全批结束后对仍缺 `imagePrompt` 的卡做二次补全并告警
- #802+#801 剧本缓存指纹统一 + 旁白压缩开关：`styleFrag` 公式此前 7 处复制（新增参数必漂移）；抽出唯一实现 `scriptFingerprint({style, compressNarration, visualMode})` 并替换全部调用点（视觉模式参数为图片小说预留，默认不改动现有缓存键）；生成内容面板新增「压缩旁白」开关（默认关闭＝忠实保留全文旁白），开关进入缓存指纹，切换后剧本自动重生成
- #793/#794/#795/#796/#797/#798/#800 保真批次：剧本提示词删除「旁白精简提炼/只允许压缩旁白」措辞，默认注入「忠实全文」硬约束（每段原文至少一条 line）；保真核对新增段落覆盖率（旁白/描写被删光也会被发现并触发一次自动重写，压缩旁白开启时该触发关闭）并在核对面板显示；CG 每章上限 1→3（按名场面，宁缺毋滥）；场景划分改为「按地点/时间切分、宁多勿并」（上限 20 仅极端保护）；分支不再每章只留 1 个、每场景保留 2-4 个选项、分支台词不再静默截断；scenes 为空改为抛错进失败项（不再用 200 字占位内容污染缓存）；单章预算从固定 4 万字改为按上下文与输出上限自适应（`chapterCharBudget`，中文约 1.5 万字起）
- 新增单测：`unit-selfcheck`、`unit-script-fingerprint`、`unit-script-policy`，并扩展 `unit-render`（跨章复位）/`unit-web-security`（私网拒绝）/`unit-script-verify`（段落覆盖）/`unit-extract-agent`（补全装箱）/`unit-sound-off`（默认开启）

### 新增（生成前图片总账 2026-09-20）
- 生成图片前先算数量：逐章生成主按钮下方常驻「图片统计：图片共 N 张：已生成 X · 待生成 Y」，整书生成面板（勾选图像阶段时）与分阶段面板同步显示；批量运行（生成选中 / 顺序补全）的确认框加入「图像待生成约 N 张」，点击前先刷新章节灯保证数字真实
- 口径与章节灯一致（每章背景/CG/物品 + 本章出场角色的人物图）；无剧本 / 图像关闭 / 未配置图像 API 分别给出明确文案，未生成剧本的章节单独计数并说明「未计入」，不把未知当 0
- 统计逻辑抽为纯函数 `src/core/imageStats.ts`（`summarizeImagePlan` / `missingImagesInChapters`）并加单测 `tests/unit-image-plan.ts`

### 修改（生成页去按钮化：整书面板与单阶段面板再收一层 2026-09-20）
- 整书生成：7 个阶段复选框默认收成一行「本次执行阶段 · 全阶段」+「调整」；章节范围收成「章节范围 · 全书」+「调整」（20+ 个章节复选框不再铺满一屏）；默认视野只剩 主按钮 + 图片总账 + 两个折叠入口
- 单阶段重跑：每行默认只有「阶段状态 + 重新生成」，意见输入框与「全量」收进该行的「意见/全量」折叠（此前每行 3 个控件、7 行共 20+ 控件）
- 生成内容：一级只留 5 个总开关（图像 / 配音 / 视频推荐位 / BGM / 环境音效）；表情差分、人物图详细度、人物动作、三视图、资料卡等立绘微调移入「高级设置」
- 新增 i18n 文案：调整 / 收起 / 全阶段 / 章节范围 / 意见/全量（5 语言齐全）

### 重构（生成页信息架构 2026-09-20）
- 顺序改为「任务在前、配置在后」：范围切换（逐章生成 / 整书生成 / 单阶段重跑）从设置抽屉移到任务区顶部，紧跟该范围的任务卡；设置抽屉默认收起，摘要显示「项目 · 图像✓ · 配音× · 旁白忠实」
- 运行日志卡只在「有日志或运行中」时出现，空闲且无日志时不再常驻一张空卡；产物抽屉保持默认收起
- 默认可见层级收敛为：页头 → 失败/守门横幅（条件出现）→ 进度（运行时）→ 范围切换 → 任务卡（含图片总账）→ 设置/日志/产物折叠行
- 设置抽屉记住展开状态（`novelforge:generate-settings-open`，默认收起；刷新/重启后保持上次状态）
- 项目栏从「嵌套折叠」改为设置内扁平一行（状态标签 + 输出目录输入 + 浏览/加载），少一层折叠
- 逐章列表的「重跑」链接在鼠标设备上默认隐藏、悬停/键盘聚焦才显示（触屏设备保持可见），20+ 行不再常驻 20+ 个链接

### 新增（表情差分走 GPT-Image 编辑模式 2026-09-21）
- 表情差分（非 normal）在 **GPT-Image 系线路**上优先走官方 `POST /v1/images/edits`：以现有立绘为原图，指令只改表情并列出「保持不变清单」（身份/发型/服装/姿势/构图/光影/绿幕底/画风）——姿势与构图不再因重画而漂移，切换表情不跳图
- **其他模型/线路保持原样**（三视图/普通立绘 + 参考图生图）；编辑端点不可用或调用失败（线路不支持/审核/网络）自动回退原路径，不影响出图
- 实现：新增 `src/api/imageEdits.ts`（`supportsImageEdits` 判定 GPT-Image 家族 + multipart 拼装 + 经 `runWithImageLimit` 复用图像并发限流）；multipart 通过 `tauri.http` 的 bodyBase64 直发，Tauri 与网页版两条传输无需 Rust/代理改动；`ImageTask.editVariant` 标记表情差分任务
- 测试：`unit-image-edits`（模型白名单、multipart 字段/字节无损、任务标记、编辑指令包含保持不变清单）；全套 **72/72**、build 通过
- 依据：OpenAI 官方 SDK `openai@7.20.0` 类型定义（本机直连 developers.openai.com 被 403，改用官方 SDK 作为官网事实来源）；顺带核实：edits 无 `moderation` 参数、`input_fidelity` 对 gpt-image-2 为「忽略」、`transparent` 对 gpt-image-2 属 preview（2.5 flare/sunburst 才正式支持）

### 修复 + 新增（游戏文案正确性与立绘半身取景 2026-09-21）
- 修复：作品标题解析错误——游戏标题/菜单/PWA 显示成了小说文件名（用户实测「175812 gbk」）。新增 `resolveProjectTitle`：导出标题 → 卡片标题（过滤纯数字/编码标记等垃圾串）→ 输出目录名 → 文件名兜底；提取与组装两处统一使用
- 修复：章节标题卡重复前缀（「第 1 章 · 第一卷 第一章 成功回避死亡结局」）——标题本身已含「第X章/第X卷」时不再拼接前缀
- 修复：重新提取后卡片 id 变化（如 `xigangjingyinyue` → 拼音/罗马音互换），旧剧本缓存仍命中，渲染时按 id 找不到角色卡 → 人名退化成内部 id（用户实测 `ouiriseri:`、`reina:`、`unknown:`）。新增**卡片指纹**（角色/物品/场景 id+名称排序哈希）进入剧本缓存键，卡片变化即自动失效重写；管线/章节灯/阶段看板/保真核对四处同源
- 新增：**立绘半身取景**——素材按全身（head-to-feet）生成，游戏内用 `setTransform` 缩放 1.75 + 下移 350 取到「头到大腿」构图（近似旧版 thighs-up 观感）；高潮台词推近改为相对取景（1.96 / 518），CG 恢复立绘后重新套用取景；`figureFraming:"full"` 可退回整身显示（组装输入已预留）
- 测试：新增 `unit-title`（标题解析与垃圾串过滤）与卡片指纹断言；`unit-render`/`unit-features` 更新取景与标题卡断言；全套 **71/71**、cargo 32 通过

### 修复 + 重做（生成页失败项销账与信息层级 2026-09-20）
- 修复（用户实测：点「重试」成功后报错仍挂在页面上）：失败列表此前只读 `projectState.lastResult.failedTasks`（项目快照，永不刷新），而管线/素材重试写的是 `failed.json`——两者脱节；且列表去重只按 `id`，图像「背景/CG」共用 scene.id 会互相抵消。现改为：**以 failed.json 为唯一事实来源**（`refreshFailedTasks()` 在每次运行结束、逐条重试后、切换/恢复项目时刷新），去重按 `failedTaskIdentity`（kind + id + 用途），并新增纯函数 `visibleFailedTasks` 与单测 `unit-failed-tasks`
- 单章剧本重试补销账：重跑成功后同步清掉内存列表、项目快照与 failed.json（此前只有图像/配音两条路径做了销账）
- 生成页去噪（重做）：三条可同时堆叠的横幅收敛为**一条**（失败项 > 视觉守门 > 续跑计划，一次只显示最高优先级 + 单一入口）
- 章节列表默认只显示**未完成 + 失败**的章节（行内只留 勾选/标题/状态，字数与详情收起），全部完成时只剩一行「全部章节已完成」；右上角「显示全部（N 章）」随时可看全量——大部头不再一屏 20+ 行
- 失败章节在列表里直接标「失败」红标，点该行「重跑 → 生成本章」即可就地重试
- 运行日志改为可折叠卡片（运行时自动展开，空闲可收起为「运行日志 · 最近 N 条」一行）
- i18n 新增 4 条（只看未完成 / 显示全部 / 全部章节已完成 / 最近 {n} 条，5 语言）
- 验证：`vue-tsc --noUnusedLocals`、**70/70 单测**、`npm run build`、浏览器探针 0 console error

### 修复（分章丢章：短编号章被误合并/误丢弃 2026-09-20）
- 用户实测：小说有 22、23 章，但章节列表只有 21 章——22/23 章正文被「碎章合并」并进了第 21 章。原因：合并只按字数阈值（<3000 字）判断，未区分「真章节」与「后记/插图/特典」；合并本意只针对附加内容
- 修复：新增真章节标题判定（`第X章/回/节/话/篇/部/幕/卷`、`N.`/`N、`、序/序章/序幕/楔子/引子/终章/尾声）——**真章节即使很短也不参与碎章合并**；合并仅对后记/插图/特典/作者的话与无题碎片生效
- 配套加固：新增「编号真章节保护」（始终生效，不依赖 keepSpecials）——被 LLM 误标为杂项的编号章节块会移出丢弃集并就地立章（漏识别时不再静默丢章）
- 测试：`unit-split-merge` 新增「短编号章不合并」「楔子不并入第一章」「误标杂项的编号章救回」三组断言
- 恢复步骤（用户侧）：生成页 →「分章设置与意见」→ 重新「AI 分章」→ 21–23 章因正文变化将自动重跑（1–20 章缓存不受影响）

### 修复（忠实全文回归 + 在途请求取消 + 图片小说 v1 补强 2026-09-20）
- 回归修复（用户实测：全章节「剧本未产出任何场景」）：忠实全文模式下长章输出极易被截断，JSON 续写/修复后回执缺少 `scenes`，而 #797 的硬失败让每个章节都暴露成失败项。现按「输出预算 × 语种字符/token × 0.65」把超长章节**分块生成**（`planScriptChunks`：按段落边界切块、无损覆盖原文、场景 id 跨块去重、CG 配额整章累计），空 `scenes` 带明确反馈**纠正重试一次**（提示模型必须输出 scenes 数组），仍失败才进失败项且错误信息含成因与建议（开启压缩旁白/换更高输出上限模型）
- 配套：`scenes 为空` 类错误归入 invalid_param（脚本层已重试，外层不再退避重跑整章，避免单章放大成 4 次整章付费调用）；「视觉守门清单缺失」提示改为中文说明性文案（首次出图会自动创建草稿，不再显示英文路径警告）
- #784 在途 HTTP 请求可取消：Rust 侧新增 `cancel_http_request`（in-flight 注册表 + tokio AbortHandle），`http_request` 接受 `requestId`；前端 `tauri.http(args, signal)` 在 abort 时调用取消（Web 运行时 fetch 同样接受中止信号）；管线与图片小说运行建立全局中止信号（`src/api/abort.ts`），点「停止」后**在途请求立即中断**，不再出现最长再跑 600s 并继续计费
- 图片小说 v1 补强：风格锚点产物作为无角色分镜的画风参考（有 identity 参考的任务不受影响）；「重试失败项」先补齐缺剧本章节再补图并重新组装；单张重生成前校验三视图存在（缺失时明确提示，不静默降级）；鉴赏室新增「分镜画廊」分组（`appreciation-data.js` 增加 `shots`，5 语言标签）
- 测试：新增剧本分块用例（小上下文 + 长章必须分块且无损）与 scenes 空错分类断言；Rust 取消命令单测；全套 69/69、cargo 32 通过

### 新增（图片小说 · 纯图片模式 v1 2026-09-20，ReqFlow #803–#813）
- 侧栏新增「图片小说」入口（异步页面）：与立绘版完全独立——独立输出目录 `<用户所选目录>/<书名>-图片版/`、独立状态与持久化（`.novel2vn/image-story.json`）、独立缓存；仅复用主项目的原文与分章（同一本小说只导入/分章一次，翻译缓存也优先复用）
- 分镜剧本通道：`scriptChapter` 支持 `mode:"imageOnly"`，按场景产出 `shots[]`（英文整幅插画 prompt + `triggerLineIndex` 切换点 + 出场角色 + 短标题），不产 CG/立绘字段；每场景张数旋钮注入提示词，超限裁剪并告警；`parseChapterScript` 新增 shots 校验（缺 prompt/越界/非整数/非数组一律判坏数据，不写缓存）
- 图片通道：`buildImageTasks` 新增 `mode:"imageOnly"`——角色三视图（identity 参考）＋ 每场景分镜（1536×1024，非绿幕）＋（可选）物品图；不产立绘/表情/动作/服装差分、不做抠图；三个旋钮（每场景张数 / 每章上限 / 总量或预算上限，0=不限，到上限暂停并提示，可提高上限续跑）；三视图失败时不为该角色生成分镜（不静默降级为纯文生图）
- 渲染分支：`renderChapter` 新增 `mode:"imageOnly"`——整族 `changeFigure`/入场动画/`setTempAnimation`/立绘特写/登场资料卡/物品图标演出全部跳过；画面按 `shot.triggerLineIndex` 以 `changeBg … -duration=400 -ease=easeInOut` 只淡入切换（#814 决策 9，不推近）；无分镜产物时保留上一张；文本框/人名/BGM/SE/选项/intro/鉴赏室解锁照常
- 组装与导出：`assembleProject` 支持 `mode:"imageOnly"`（分镜复制进 `game/background/`、其余产物与立绘版同构）；新页面自带「预览」（复用 Rust 预览服务）与「导出 zip」（Tauri 存盘/网页版直下载），lint 的 `changeBg` 校验直接覆盖分镜引用
- 页面（沿用生成页「任务在前、配置折叠」版式）：项目/目录行、张数与费用（预计最多 N 张 ≈ X 元，实时随旋钮刷新）、三个张数旋钮与画风、高级设置（种子/自检/物品图/风格锚点/目标语言）、开始/停止、进度（阶段 + 张数 x/y）与运行日志、失败列表与「重试失败项」、分镜结果网格（按章节分组、懒加载、放大预览、单张强制重生成）
- 数据与映射：新增 `Shot` 类型、`SceneJSON.shots`、`ImageTask.kind:"shot"`、`RenderAssets.shot`、`AssetMap.shot`（旧 `assets.json` 缺该键时按空处理，向后兼容）
- 测试：`unit-image-story-tasks`（任务集合/三旋钮裁剪/参考角色）、`unit-image-story-render`（无立绘指令、按 trigger 切换、立绘版回归）、`unit-image-story-validate`（shots 坏数据）、指纹隔离断言（sprite ≠ imageOnly）；全套 69/69 通过
- 文档：实施计划 `.specs/tasks/todo/image-story.feature.md`（P1–P6 分批、验收、风险、回滚）


## [0.3.0] - 2026-09-07

### 新增
- 项目列表：每个项目绑定小说×输出目录，切换自动换目录和全套状态；导入不同小说提示分独立目录，防缓存串味
- 视觉守门全局重新生成改为批量并行（默认 3 并发，可跟图像通道配置走），附进度条与单角色失败隔离
- 提取分段手动旋钮（生成页 Agent 模式下「提取分段」，0=自动）：中转网关有单请求超时墙时可调小（如 40000）绕过 HTTP 500
- 角色描述重生成请求 JSON 模式 + 括号平衡解析：模型在 JSON 后跟闲聊不再报“不是合法 JSON”
- LLM opt-in 类 403 自动追加中文指引（打开链接完成 opt-in 或换模型）

### 修复
- 中文小说提取分段预算按实际语种估算（约 0.6 字符/token，以前按 1.5 算导致 80 万字巨段触发网关 500），另加 15 万字硬上限
- 风格描述里的背景词会跟绿幕后缀冲突：三视图/立绘/动作/服装/物品提示词统一先剥背景词（背景/CG/锚点不受影响）
- 剧本核对报告在重写后沿用旧结果：新鲜生成与自动重写后强制重算
- 换装无服装图时误屏蔽表情差分；配音孤儿认领批量写映射、健康计数口径对齐
- IPv6 本地 Host（[::1]:5173）被误判非本地导致 403；视觉守门同步仅删多余条目时不再强制要求图像 Key
- .gitignore 拼写修正（.novel2n/ → .novel2vn/，运行时缓存此前实际未被忽略）

## [0.2.0] - 2026-08-16

### 新增
- 移除所有内容数量上限（0=不限量），取消预算超限自动中止，改为失败任务清单 + 定向重试
- 服装/形态变体立绘、音效（SE）、演出增强、鉴赏（Appreciation）画廊
- 提取 Agent 模式、分阶段状态看板、角色不限量、各 API 通道独立并发控制
- API Key 使用系统钥匙串加密存储（keyring），避免明文落盘
- 资源缩略图懒加载（LazyThumb + 资产缩略图缓存），性能优化
- 统一路径/错误处理/性能工具模块；数据校验（dataValidation）与资产映射（assetMap）

### 修复
- 图像提示词压缩、参考图能力探测与降级（REFERENCE_UNSUPPORTED / REFERENCE_MISSING）
- 移除 AI 抠图模型，改为纯代码色度键（连通 flood-fill + 去绿边 + 羽化）
- 服务端 UTF-8 charset、文字生成彻底串行（LLM 全局并发 1）消除 error sending request
- 按模型上下文动态决定输入预算（128K 默认），避免推理模型输出截断

### 文档
- README 增加游戏实机截图（标题页 / 剧情场景 / 名场面 CG）与下载徽章

---

## [Unreleased]

### 新增
- 统一路径处理工具模块 `src/utils/path.ts`
  - `normalizePath()` - 标准化路径分隔符
  - `basename()` - 获取文件名
  - `dirname()` - 获取目录路径
  - `joinPath()` - 安全拼接路径
  - `cleanPath()` - 清理路径
  - `safeFilename()` - 生成安全文件名
  - `extname()` - 获取扩展名
  - `basenameWithoutExt()` - 获取不含扩展名的文件名

- 错误处理工具模块 `src/utils/errors.ts`
  - `NovelForgeError` - 自定义错误类
  - `ErrorCode` - 错误类型枚举
  - `createError()` - 创建结构化错误
  - `formatError()` - 格式化用户友好的错误消息
  - `safeAsync()` - 安全执行异步操作
  - `retry()` - 自动重试机制
  - `logError()` - 错误日志

- 性能优化工具模块 `src/utils/performance.ts`
  - `debounce()` - 防抖函数
  - `throttle()` - 节流函数
  - `createBatcher()` - 批量处理
  - `memoize()` - 结果缓存
  - `ConcurrencyLimiter` - 并发控制
  - `PerformanceTimer` - 性能计时器
  - `measurePerformance()` - 性能监控装饰器

### 优化
- `src/utils/template.ts` - 使用统一路径工具和错误处理
- `src/core/cache.ts` - 使用 `cleanPath()` 和 `safeFilename()`
- `src/core/project.ts` - 所有路径操作标准化，提高可读性
- `src/core/render.ts` - 使用统一的 `basename()` 函数
- `src/core/chapters.ts` - 使用 `basename()` 和 `basenameWithoutExt()`
- `src/utils/vfsWeb.ts` - 使用统一的路径处理函数

### 修复
- 按钮联动与并发安全：单章节队列运行期间锁定「开始生成/整书生成」与执行方式切换，避免误触发整书＋配音的昂贵全流程或让队列失去停止入口；运行期间禁用「浏览…/加载该项目」，防止中途切换项目把管线结果写进别的项目
- 文字显示接近真实 Galgame：长旁白/对话按句读拆成多条消息（一屏一句、逐句翻页）；「X说/叹气道：」类动作标签不再独立成行（并入下句对话并推断说话人/情绪）；内心独白用「」区分；有配音的台词保持单条不掐断语音（新增 render 单测 #9-13，详见 docs/AI_GAL_TEXT_GAP.md）
- 视觉守门支持多套服装：每套服装自动生成独立三视图锚点（以默认装三视图为身份参考，保证换装后身份/服装一致），换装立绘改引对应服装三视图；面板可查看/单套重生成；服装增删与重生成纳入指纹使视觉守门过期待确认（详见 docs/VISUAL_BIBLE_MULTI_COSTUME.md）
- 关闭「图像」时章节完成只看剧本：章节灯不再永远显示图像未完成，「顺序生成未完成」可正常收敛
- 素材页「全选本区」补选服装差分立绘（此前服装不随全选进入批量重生成）
- 全局重新生成/同步卡片漏建服装锚点：此前批量重建只画主三视图、同步补建不同步服装，导致服装锚点新旧混杂（旧白底与新绿幕并存）；现与单角色重建/创建草稿同语义，以新默认装三视图为身份参考逐套重建，无服装时清理过期锚点
- 面板此前只列已有锚点的服装：现以卡片服装为准全量列出，缺锚点的显示「未生成」＋一键生成入口；卡片已删服装的旧残留只展示不给重建按钮
- 重生成点击无日志：素材页 14 处入口在前置拦截（任务执行中/无项目数据）时静默返回，抠图/恢复映射备份/视觉守门三处重生成入口亦然；现全部改为先记警告日志再返回，每次点击至少留一条日志
- 「确认此角色」慢：批准校验逐角色串行读盘（9 角色 20+ 次往返），改为按角色并发校验（错误顺序不变）；确认成功日志带用时，方便定位后续瓶颈

### 文档
- 新增 `OPTIMIZATION_SUMMARY.md` - 详细的优化总结文档
- 新增 `CHANGELOG.md` - 项目变更日志

---

## [0.1.0] - 2026-08-05

### 修复
- 修复 WebGAL 模板路径在 Windows 下的查找问题
  - 问题：Windows 返回的路径使用反斜杠 `\`，导致模板查找失败
  - 解决：在 `src/utils/template.ts` 中标准化路径为正斜杠
  - 提交：f309002

### 新增
- 初始版本发布
- 小说转视觉小说核心功能
- AI 驱动的角色提取和场景脚本生成
- 图片生成和语音合成集成
- WebGAL 引擎项目导出
- 项目预览功能
- 多章节处理
- 素材缓存机制

### 特性
- 🎨 Vue 3 + TypeScript 前端
- ⚡ Vite 构建工具
- 🦀 Tauri 2.0 桌面应用框架
- 🤖 OpenAI Compatible API 支持
- 🎭 角色卡片和场景分析
- 🖼️ AI 图片生成（背景、CG、立绘）
- 🔊 AI 语音合成
- 📦 WebGAL 项目打包
- 🌐 网页版导出（ZIP）
- 💾 IndexedDB 浏览器存储支持

---

## 版本说明

### 版本号规则

遵循语义化版本 `MAJOR.MINOR.PATCH`：

- **MAJOR**：不兼容的 API 变更
- **MINOR**：向后兼容的功能新增
- **PATCH**：向后兼容的问题修复

### 变更类型

- **新增 (Added)** - 新功能
- **优化 (Changed)** - 现有功能的变更
- **弃用 (Deprecated)** - 即将移除的功能
- **移除 (Removed)** - 已移除的功能
- **修复 (Fixed)** - Bug 修复
- **安全 (Security)** - 安全性修复

---

**仓库：** https://github.com/carloncc/NovelForge  
**作者：** 陈俊龙 <1599692505@qq.com>
