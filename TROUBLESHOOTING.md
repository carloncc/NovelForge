# NovelForge 常见问题排查指南

**版本：** v0.1.1  
**更新：** 2026-08-06

---

## 🚨 常见问题和解决方案

### 问题 1：无法生成角色卡片和视觉小说

#### 症状
- 点击"开始生成"没有反应
- 生成过程卡住或报错
- 提示"未找到 WebGAL 引擎模板"

#### 原因分析

**1. 未配置 API Key（最常见）**

NovelForge 需要调用 AI API 来生成内容。如果没有配置 API，将无法正常工作。

**解决方案：**

a) 进入**配置页面**
b) 配置至少一个**文本 LLM API**（必需）：
   - Base URL：如 `https://api.openai.com/v1`
   - API Key：你的 API 密钥
   - Model：如 `gpt-4o-mini` 或 `gpt-3.5-turbo`

c) 可选配置：
   - **图像 API**：生成立绘、背景、CG（推荐）
   - **TTS 配音**：生成角色配音（可选）

d) 点击**测试连接**按钮验证配置

**演示模式：**
- 如果不配置 API，系统会使用内置示例数据
- 演示模式仅用于测试界面，不能处理真实小说

---

**2. WebGAL 模板缺失**

#### 症状
```
未找到 WebGAL 引擎模板（C:/path/to/templates/webgal）
```

#### 解决方案

模板应该在应用资源目录中。如果缺失：

**方法 1：重新下载模板**
```bash
cd NovelForge-main
pnpm run prepare:template
```

**方法 2：检查模板是否存在**
```bash
# 开发环境
ls src-tauri/templates/webgal/

# 检查必需文件
src-tauri/templates/webgal/index.html
src-tauri/templates/webgal/assets/
src-tauri/templates/webgal/game/
```

**方法 3：重新构建应用**
```bash
pnpm tauri build
```

---

**3. 路径问题（Windows）**

#### 症状
- 模板路径显示混用 `\` 和 `/`
- 提示路径不存在

#### 解决方案

v0.1.1 已修复此问题。如果仍有问题：
- 确保使用最新版本
- 检查输出目录路径是否正确
- 避免路径中包含特殊字符

---

### 问题 2：生成过程中断或失败

#### 症状
- 生成到一半停止
- 出现 API 错误
- 超出预算提示

#### 解决方案

**1. 检查日志**
- 切换到**日志**标签页
- 查看具体错误信息
- 点击"复制日志"保存调试信息

**2. API 问题**
- 验证 API Key 是否有效
- 检查账户余额
- 确认网络连接正常
- 查看 API 速率限制

**3. 预算设置**
```
超出预算 ¥X（本次累计 ¥Y），已中止
```
- 在生成选项中提高预算限制
- 或取消预算限制（设为 0）

**4. 使用缓存恢复**
- NovelForge 自动缓存已生成的内容
- 重新运行时会跳过已完成的部分
- 勾选"跳过缓存"可强制重新生成

---

### 问题 3：导出失败

#### 症状
- 无法打包 ZIP
- 提示导出错误

#### 解决方案

**1. 确保项目已生成**
- 必须先在"生成项目"页面完成生成
- 查看输出目录是否有内容

**2. 检查磁盘空间**
- 确保有足够的可用空间
- 典型项目大小：50-500MB

**3. 检查输出目录权限**
- 确保有写入权限
- 避免使用系统保护目录

---

### 问题 4：无法导入小说

#### 症状
- 选择文件后没有反应
- 提示编码错误

#### 解决方案

**1. 文件格式要求**
- 必须是 `.txt` 文件
- 编码：UTF-8（推荐）或 GBK
- 大小：建议 < 500KB

**2. 文件内容要求**
- 纯文本格式
- 使用空行分隔段落
- 章节标题格式：
  ```
  第一章 开始
  第二章 转折
  序章
  尾声
  ```

**3. 编码问题**
- 如果出现乱码，用记事本另存为 UTF-8 编码
- Windows：记事本 → 另存为 → 编码选择 UTF-8

---

## 🔍 逐步诊断流程

### 步骤 1：验证基础配置

```
✅ 检查清单：
[ ] 已安装 NovelForge 应用
[ ] 已配置至少一个文本 LLM API
[ ] API 配置通过测试连接
[ ] 有可用的 API 额度
```

### 步骤 2：测试完整流程

1. **导入示例小说**
   - 使用"加载示例小说"功能
   - 验证小说是否正确显示

2. **配置生成选项**
   - 设置输出目录
   - 选择要生成的内容（图像、配音）
   - 设置预算（建议先设为 0 测试）

3. **开始生成**
   - 点击"开始生成"
   - 观察进度指示器
   - 查看日志输出

4. **检查输出**
   - 切换到"角色卡"标签
   - 查看生成的角色、场景、物品
   - 切换到"剧本"标签查看生成的脚本

### 步骤 3：问题定位

**如果卡在"提取"阶段：**
- API Key 未配置或无效
- 网络连接问题
- API 服务不可用

**如果卡在"剧本"阶段：**
- 小说内容太长（超过 70000 字符）
- API 超时
- 模型不支持 JSON 输出

**如果卡在"图像"阶段：**
- 图像 API 未配置
- 图像生成失败（查看"失败项"）
- 预算不足

**如果卡在"组装"阶段：**
- WebGAL 模板缺失
- 输出目录权限问题
- 磁盘空间不足

---

## 📋 日志分析

### 正常日志示例

```
[14:23:45] [提取] 开始提取角色/场景/物品卡…
[14:24:12] [提取] 提取完成：3 角色 / 5 场景 / 2 物品
[14:24:13] [剧本] 生成第 1 章剧本：第一章 相遇
[14:24:45] [剧本] 生成第 2 章剧本：第二章 冒险
[14:24:46] [图像] 开始处理图像素材…
[14:25:30] [图像] 生成完成：15 张图片
[14:25:31] [配音] 跳过（未启用配音）
[14:25:32] [组装] 复制引擎文件…
[14:25:35] [完成] 全部完成！
```

### 错误日志分析

**错误 1：API Key 无效**
```
[提取] 错误：401 Unauthorized
```
→ 检查 API Key 是否正确

**错误 2：网络超时**
```
[剧本] 错误：timeout / network error
```
→ 检查网络连接，重试

**错误 3：模板缺失**
```
[组装] 错误：未找到 WebGAL 引擎模板
```
→ 运行 `pnpm prepare:template`

**错误 4：超出预算**
```
[剧本] 错误：超出预算 ¥10（本次累计 ¥12.5）
```
→ 提高预算限制或勾选"跳过缓存"

---

## 🛠️ 开发者调试

### 查看详细错误

**开发模式：**
```bash
pnpm tauri dev
```
打开浏览器开发者工具（F12）查看控制台错误

**查看 Tauri 日志：**
- Windows: `%APPDATA%\com.novelforge.app\logs\`
- macOS: `~/Library/Logs/com.novelforge.app/`
- Linux: `~/.local/share/com.novelforge.app/logs/`

### 清除缓存重试

```bash
# 删除项目缓存
rm -rf <output_dir>/.novel2vn/cache/

# 或勾选"跳过缓存"选项
```

---

## 💡 最佳实践

### 1. 首次使用

1. 配置 API（必需）
2. 加载示例小说测试
3. 使用演示模式验证流程
4. 导入真实小说开始创作

### 2. 降低成本

- 使用更便宜的模型（如 gpt-3.5-turbo）
- 先生成剧本，检查后再生成图像
- 复用已生成的内容（缓存）
- 分章节逐步生成

### 3. 提高质量

- 使用更强大的模型（如 gpt-4）
- 提供高质量的小说文本
- 手动编辑角色卡片
- 上传参考素材

---

## 📞 获取帮助

如果问题仍未解决：

1. **查看完整日志**
   - 生成页面 → 日志标签 → 复制日志

2. **提交 Issue**
   - 仓库：https://github.com/carloncc/NovelForge/issues
   - 包含：日志、配置信息、重现步骤

3. **查看文档**
   - README.md - 项目介绍
   - OPTIMIZATION_SUMMARY.md - 优化说明
   - DEVELOPER_TOOLS.md - 开发者工具

---

**最后更新：** 2026-08-06  
**维护者：** NovelForge 团队
